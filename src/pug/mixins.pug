//-  миксины

//- добавление svg из спрайта
mixin svg(name, iconClass)
  if iconClass
    svg(class= iconClass)
      use(xlink:href=`images/sprite.svg#${name}`)
  else
    svg
      use(xlink:href=`images/sprite.svg#${name}`)
